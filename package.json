{
  "name": "gwl-creators-hub-2.0",
  "version": "2.0.0",
  "private": true,
  "description": "GWL Creators Hub — root scripts to drive the backend + frontend apps",
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "db:setup": "npm --prefix backend run db:push && npm --prefix backend run db:seed",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev": "concurrently -n backend,frontend -c cyan,magenta \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "build": "npm --prefix backend run build && npm --prefix frontend run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
