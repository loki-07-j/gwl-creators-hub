# Copy to .env. Backend API base URL used by the frontend (axios).
#   Absolute  → call the backend directly by host:port (backend CORS must allow this origin)
#   Relative  → leave as "/api/v1" to use the Vite dev proxy (same-origin, simplest)
VITE_API_URL=http://localhost:5001/api/v1
